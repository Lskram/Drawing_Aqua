# คู่มือใช้งาน Heartopia Image Painter

อัปเดต: 2026-05-08

คู่มือนี้เขียนสำหรับการใช้งานจริงกับเกม Heartopia โดยแยกเป็น 2 โปรแกรมหลัก:

1. `Heartopia Image Painter` ใช้วาดภาพลงในเกมด้วยการควบคุมเมาส์อัตโนมัติ
2. `Clothing Template Manager` ใช้เตรียมรูปสำหรับเสื้อผ้า เช่น Dress และ T-Shirt ก่อนนำไปวาดจริง

> โปรแกรมนี้ไม่ได้แก้ไฟล์เกมโดยตรง แต่ควบคุมเมาส์เพื่อช่วยวาดในหน้าจอเกม ดังนั้นตำแหน่งจอ ขนาดหน้าต่างเกม และการเลือกพื้นที่ Canvas ต้องตรงก่อนเริ่มวาด

---

## 1. ภาพรวมการใช้งาน

### ใช้โปรแกรมไหนทำอะไร

| โปรแกรม | ใช้ทำอะไร | เปิดจากไฟล์ |
|---|---|---|
| Heartopia Image Painter | เอารูปเข้าโปรแกรม เลือกพื้นที่ในเกม ตั้งสี แล้วให้โปรแกรมวาดลงเกม | `main.py` |
| Clothing Template Manager | เตรียมรูปให้พอดีกับ Template เสื้อผ้า เช่น Dress Front Part, Back Part, Innerwear | `dress_template_manager.py` |

### Workflow ที่แนะนำแบบสั้น

1. ถ้าจะวาดรูปทั่วไป เช่น 1:1, 16:9, 9:16 ให้ใช้ `Heartopia Image Painter` ได้เลย
2. ถ้าจะวาดเสื้อผ้า เช่น Dress หรือ T-Shirt ให้เตรียมรูปใน `Clothing Template Manager` ก่อน
3. Export รูปจาก Template Manager ออกมา
4. Import รูปที่ Export แล้วเข้า `Heartopia Image Painter`
5. เข้าเกม เปิดหน้าวาดรูปหรือหน้าวาดเสื้อผ้า
6. กด `Select canvas area...` แล้วลากพื้นที่วาดในเกม
7. ตั้งค่าสีและตำแหน่งปุ่มเครื่องมือ
8. กด `Paint now`

---

## 2. คำศัพท์ที่ควรรู้

| คำ | ความหมาย |
|---|---|
| Canvas area | พื้นที่ในเกมที่โปรแกรมจะคลิกวาด |
| Preset | ประเภทพื้นที่วาด เช่น 1:1, 16:9, T-Shirt, Dress |
| Precision | ความละเอียดของช่องวาดในเกม เช่น Small, Medium, Big, Super Large |
| Part | ส่วนของเสื้อผ้าที่จะวาด เช่น Front Part, Back Part, Innerwear |
| Palette | ชุดสีในเกม |
| Shade | เฉดสีที่อยู่ใน Palette |
| Mask | รูปร่างขอบเขตของเสื้อผ้า ใช้ตัดภาพให้ไม่ล้นออกนอกทรงชุด |
| Calibration | การปรับ Mask หรือรูปให้ตรงกับเกมจริง เช่น ขยับซ้าย ขวา ย่อ ขยาย |
| Export | บันทึกรูปที่เตรียมเสร็จออกมาเป็นไฟล์ใหม่ |

---

# โปรแกรมที่ 1: Heartopia Image Painter

## 3. หน้าที่ของโปรแกรมหลัก

โปรแกรมนี้ทำหน้าที่แปลงรูปภาพให้เป็นจุดสี แล้วคลิกวาดลงในเกม Heartopia ตามตำแหน่งที่ผู้ใช้เลือกไว้

เหมาะกับ:

- วาดภาพทั่วไป
- วาดภาพใส่กรอบ
- วาด T-Shirt
- วาด Dress หลังจากเตรียม Template แล้ว
- ลบ Canvas
- วาดแบบแยกสีหรือวาดตามแถว

---

## 4. วิธีเปิดโปรแกรมหลัก

เปิดด้วยไฟล์:

```powershell
python main.py
```

ถ้าเมาส์คลิกในเกมไม่ได้ หรือเกมรันด้วยสิทธิ์สูงกว่า ให้เปิดแบบ Administrator

---

## 5. ส่วน Import Image

### ปุ่ม `Import image...`

ใช้เลือกรูปภาพที่จะนำมาวาด

ตัวอย่าง:

- ต้องการวาดรูปกระต่าย ให้กด `Import image...` แล้วเลือกรูปกระต่าย
- ถ้าวาด Dress ให้ Import รูปที่ Export มาจาก `Clothing Template Manager`

ข้อควรระวัง:

- ถ้ารูปมีพื้นหลังเยอะ ควรเปิด `Auto-crop empty border`
- ถ้ารูปเป็นเสื้อผ้าที่เตรียม Template แล้ว ไม่ควร Crop มั่ว เพราะอาจทำให้ Mapping เคลื่อน

---

## 6. ส่วน Canvas Preset

### ช่อง `Canvas preset`

ใช้เลือกประเภทงานที่จะวาด

| Preset | ใช้เมื่อไหร่ |
|---|---|
| `1:1` | ภาพสี่เหลี่ยมจัตุรัส |
| `16:9` | ภาพแนวนอน |
| `9:16` | ภาพแนวตั้ง |
| `4:3` | ภาพแนวนอนแบบเก่า |
| `3:4` | ภาพแนวตั้งแบบเก่า |
| `T-Shirt` | วาดเสื้อยืด |
| `Dress` | วาดชุด Dress |

ตัวอย่าง:

- ถ้าหน้าเกมเลือกกรอบรูป 16:9 ให้เลือก `16:9`
- ถ้าหน้าเกมเลือก Dress Front Part ให้เลือก `Dress` แล้วเลือก Part เป็น `Front Part`

---

## 7. ส่วน Precision

### ช่อง `Precision`

ใช้เลือกความละเอียดของช่องวาดในเกม

| Precision | ความหมายโดยใช้งาน |
|---|---|
| `Small` | จุดน้อย วาดเร็ว รายละเอียดน้อย |
| `Medium` | สมดุลระหว่างเวลาและรายละเอียด |
| `Big` | จุดเยอะขึ้น ภาพละเอียดขึ้น |
| `Super Large` | จุดเยอะที่สุด ใช้เวลานานที่สุด |

ตัวอย่าง:

- รูปง่าย ๆ เช่น โลโก้ ใช้ `Medium`
- รูปตัวละครหรือรูปคน ใช้ `Big` หรือ `Super Large`
- ถ้าต้องการทดสอบก่อน ให้ใช้ `Small`

---

## 8. ส่วน Part สำหรับเสื้อผ้า

เมื่อเลือก Preset เป็น `Dress` หรือ `T-Shirt` จะมีช่อง `Part`

### Dress

| Part | ใช้วาดส่วนไหน |
|---|---|
| `Front Part` | ด้านหน้าชุด |
| `Back Part` | ด้านหลังชุด |
| `Innerwear` | ส่วนด้านใน |

### T-Shirt

| Part | ใช้วาดส่วนไหน |
|---|---|
| `Front` | ด้านหน้าเสื้อ |
| `Back` | ด้านหลังเสื้อ |
| `Left Sleeve` | แขนซ้าย |
| `Right Sleeve` | แขนขวา |

ตัวอย่าง:

ถ้าในเกมเลือก `Front Part` แต่ในโปรแกรมเลือก `Back Part` ภาพจะไม่ตรงแน่นอน ต้องเลือกให้ตรงกันทั้งเกมและโปรแกรม

---

## 9. ปุ่ม `Add/Edit part...`

ใช้เพิ่มหรือแก้ข้อมูล Part เอง

ใช้เมื่อ:

- เกมมีเสื้อผ้าประเภทใหม่
- ต้องการเพิ่ม Part ที่โปรแกรมยังไม่มี
- ต้องการทดลองขนาด Canvas ใหม่

โดยทั่วไปถ้าวาดแค่ Dress หรือ T-Shirt ไม่จำเป็นต้องใช้ปุ่มนี้

---

## 10. ปุ่ม `Remove part`

ใช้ลบ Part ที่เพิ่มเอง

ข้อควรระวัง:

- อย่าลบ Part มาตรฐาน ถ้ายังไม่แน่ใจ
- ถ้าลบผิด อาจต้องตั้งค่าใหม่

---

## 11. ปุ่ม `Select canvas area...`

เป็นปุ่มสำคัญที่สุดปุ่มหนึ่ง

ใช้ลากเลือกพื้นที่วาดจริงในเกม

ขั้นตอน:

1. เปิดหน้า Canvas ในเกม
2. เลือก Part ในเกมให้ตรงกับโปรแกรม
3. กด `Select canvas area...`
4. ลากครอบพื้นที่สีส้ม หรือพื้นที่วาดจริง
5. ตรวจดูข้อความ `Canvas: x=..., y=..., w=..., h=...`

ตัวอย่าง:

ถ้าเกมแสดง Dress Front Part อยู่ ให้ลากครอบเฉพาะพื้นที่ส้มของ Front Part ให้เต็มที่สุด ไม่รวมปุ่มด้านบน ไม่รวมขอบหน้าต่างเกม

ปัญหาที่พบบ่อย:

- ลากไม่เต็มพื้นที่ ภาพจะถูกบีบหรือเหลือขอบ
- ลากเกินพื้นที่ ภาพจะคลิกนอก Canvas
- เปิด Zoom ในเกมไม่เท่ากับตอนตั้งค่า ภาพอาจเคลื่อน

---

# Smart Image Prep

## 12. ช่อง `Fit`

กำหนดวิธีเอารูปต้นฉบับไปใส่ใน Canvas

| ค่า | ผลลัพธ์ |
|---|---|
| `Smart` | โปรแกรมพยายามจัดให้เหมาะที่สุด |
| `Stretch` | ยืดรูปให้เต็มพื้นที่ อาจเสียสัดส่วน |
| `Contain` | ใส่รูปทั้งใบ ไม่ตัดรูป อาจมีขอบว่าง |
| `Cover` | ให้เต็มพื้นที่ อาจตัดบางส่วนของรูป |

ตัวอย่าง:

- รูปคนเต็มตัว ใช้ `Contain` เพื่อไม่ให้หัวหรือเท้าหาย
- รูปลายผ้า ใช้ `Cover` เพื่อให้เต็มพื้นที่
- รูปที่เตรียมจาก Template แล้ว ใช้ `Smart` หรือค่าเดิมที่โปรแกรมตั้งไว้

---

## 13. ตัวเลือก `Auto-crop empty border`

ตัดขอบว่างรอบรูปอัตโนมัติ

ควรเปิดเมื่อ:

- รูปมีพื้นหลังโปร่งใสหรือขอบว่างเยอะ
- รูปเล็กอยู่กลางภาพใหญ่

ควรปิดเมื่อ:

- รูปถูกเตรียมมาเป็น Template แล้ว
- ต้องการรักษาตำแหน่งภาพแบบเป๊ะ ๆ

ตัวอย่าง:

ถ้ารูปแมวอยู่กลางไฟล์ขนาดใหญ่และมีขอบขาวเยอะ เปิดตัวนี้จะช่วยให้แมวใหญ่ขึ้นใน Canvas

---

## 14. ตัวเลือก `Map preview to captured palette`

จำลองภาพ Preview ให้ใช้สีที่จับมาจากเกมแล้ว

ใช้เพื่อดูว่าเมื่อวาดจริง สีจะประมาณไหน

ตัวอย่าง:

ถ้ารูปต้นฉบับมีสีชมพู 20 เฉด แต่ในเกมมีสีชมพูแค่ 5 เฉด Preview จะบอกให้เห็นว่าภาพจะถูกลดสีเป็นแบบไหน

---

## 15. ตัวเลือก `Dither palette-mapped image`

กระจายจุดสีเพื่อหลอกตาให้ภาพดูไล่เฉดดีขึ้น

เหมาะกับ:

- รูปภาพจริง
- รูปคน
- รูปสัตว์
- รูปที่มีแสงเงาเยอะ

ไม่เหมาะกับ:

- โลโก้
- ตัวอักษร
- ลายเส้นคม ๆ

ตัวอย่าง:

ถ้ารูปกระต่ายมีขนไล่เฉด เปิด Dither จะดูนุ่มขึ้น แต่ถ้าเป็นตัวหนังสือ อาจทำให้ขอบแตก

---

## 16. ช่อง `Background: #FFFFFF`

ตั้งค่าสีพื้นหลังที่ใช้เติมส่วนโปร่งใส

ตัวอย่าง:

- ถ้าต้องการพื้นขาว ใช้ `#FFFFFF`
- ถ้าต้องการพื้นดำ ใช้ `#000000`
- ถ้ารูป PNG โปร่งใส แต่ในเกมไม่มีความโปร่งใส โปรแกรมจะใช้สีนี้แทน

---

## 17. ปุ่ม `Re-apply to loaded image`

ใช้ประมวลผลรูปใหม่หลังจากเปลี่ยนค่า Fit, Crop, Dither, Background หรือค่า Dress mapping

ตัวอย่าง:

1. Import รูป
2. เปลี่ยน `Fit` จาก `Contain` เป็น `Cover`
3. กด `Re-apply to loaded image`
4. ดู Preview ใหม่

---

# Dress Mapping ในโปรแกรมหลัก

ส่วนนี้จะเห็นเมื่อใช้ Preset `Dress`

## 18. `Dress image scale`

ใช้ย่อหรือขยายรูปสำหรับ Dress

ตัวอย่าง:

- ถ้ารูปเล็กเกินไป ให้เพิ่มค่า Scale
- ถ้ารูปล้นออกนอกทรงชุด ให้ลดค่า Scale

---

## 19. `Offset X`

ขยับรูปซ้ายหรือขวา

| อาการ | วิธีแก้ |
|---|---|
| รูปไปทางซ้ายเกิน | เพิ่ม Offset X |
| รูปไปทางขวาเกิน | ลด Offset X |

---

## 20. `Offset Y`

ขยับรูปขึ้นหรือลง

| อาการ | วิธีแก้ |
|---|---|
| รูปสูงเกิน | เพิ่ม Offset Y เพื่อลง |
| รูปต่ำเกิน | ลด Offset Y เพื่อขึ้น |

---

## 21. ปุ่ม `Reset`

รีเซ็ต Scale และ Offset ของ Dress กลับค่าเริ่มต้น

ใช้เมื่อ:

- ปรับจนเละ
- ต้องการเริ่มตั้งตำแหน่งใหม่

---

## 22. ปุ่ม `Export Dress templates...`

ใช้เปิดหรือส่งต่อไปขั้นตอน Export Template สำหรับ Dress

ถ้าต้องการจัดการละเอียดกว่า ให้ใช้โปรแกรม `Clothing Template Manager`

---

# Color Configuration

## 23. ปุ่ม `Set shades-panel button`

ใช้ระบุตำแหน่งปุ่มเปิดแผงเฉดสีในเกม

ขั้นตอน:

1. กด `Set shades-panel button`
2. คลิกตำแหน่งปุ่มแผงสีในเกม
3. โปรแกรมจำตำแหน่งนี้ไว้

ถ้าตั้งผิด โปรแกรมจะเปิดแผงสีไม่ได้

---

## 24. ปุ่ม `Set back button`

ใช้ระบุตำแหน่งปุ่มย้อนกลับจากแผงสี

ใช้เมื่อโปรแกรมต้องเลือกสีแล้วกลับมาวาดต่อ

---

## 25. ปุ่ม `Show main-color overlay`

แสดงตัวช่วยทับหน้าจอเพื่อดูตำแหน่งสีหลัก

ใช้สำหรับเช็กว่าสีที่โปรแกรมจะเลือกตรงกับสีในเกมหรือไม่

---

## 26. ปุ่ม `Set paint tool button`

ใช้ระบุตำแหน่งปุ่มพู่กันหรือเครื่องมือวาดในเกม

ถ้าตั้งผิด โปรแกรมอาจคลิกเครื่องมืออื่นแทน

---

## 27. ปุ่ม `Set bucket tool button`

ใช้ระบุตำแหน่งปุ่มถังสีในเกม

ใช้ร่วมกับการเติมพื้นที่สีใหญ่

ตัวอย่าง:

ถ้าภาพมีพื้นหลังสีเดียวเยอะ โปรแกรมสามารถใช้ Bucket เติมก่อนเพื่อลดเวลา

---

## 28. ปุ่ม `Set eraser tool button`

ใช้ระบุตำแหน่งปุ่มยางลบในเกม

ใช้กับปุ่ม `Erase canvas`

---

## 29. ปุ่ม `Set eraser thickness + button`

ใช้ระบุตำแหน่งปุ่มเพิ่มขนาดยางลบ

ช่วยให้ลบพื้นที่ใหญ่ได้เร็วขึ้น

---

## 30. ปุ่ม `Setup new color...`

ใช้เพิ่มสีจากเกมเข้าโปรแกรม

วิธีคิดง่าย ๆ:

- โปรแกรมต้องรู้ว่าถ้าจะใช้สีแดง ต้องคลิกตรงไหนในเกม
- ปุ่มนี้ใช้สอนโปรแกรมว่าตำแหน่งสีแต่ละสีอยู่ตรงไหน

ขั้นตอนโดยทั่วไป:

1. เปิดแผงสีในเกม
2. กด `Setup new color...`
3. เลือกหรือจับตำแหน่งสีตามที่โปรแกรมให้ทำ
4. บันทึกสี

---

## 31. ปุ่ม `Remove selected`

ลบสีที่เลือกออกจากรายการสีของโปรแกรม

ใช้เมื่อ:

- จับสีผิด
- ไม่ต้องการให้โปรแกรมใช้สีนั้น

---

## 32. ปุ่ม `Fix colors: swap R/B`

แก้ปัญหาสีแดงกับน้ำเงินสลับกัน

ใช้เมื่อ:

- Preview สีเพี้ยนแบบแดงกลายเป็นน้ำเงิน
- ไฟล์ภาพหรือระบบจับสีอ่านค่า BGR/RGB สลับกัน

---

# Timing / Reliability

## 33. `Mouse move duration`

เวลาที่เมาส์ใช้เลื่อนจากจุดหนึ่งไปอีกจุด

| ค่า | ผล |
|---|---|
| ต่ำ | เร็ว แต่อาจพลาดถ้าเครื่องหน่วง |
| สูง | ช้ากว่า แต่นิ่งกว่า |

---

## 34. `Mouse down hold`

เวลาที่กดเมาส์ค้างต่อหนึ่งคลิก

ถ้าเกมไม่รับคลิก ให้เพิ่มค่านี้เล็กน้อย

---

## 35. `After each click delay`

ดีเลย์หลังคลิกแต่ละครั้ง

ถ้าเกมหน่วงหรือคลิกไม่ติด ให้เพิ่มค่านี้

---

## 36. `After opening shades panel`

รอหลังเปิดแผงสี

ถ้าแผงสีเปิดช้า ให้เพิ่มค่า

---

## 37. `After selecting shade`

รอหลังเลือกเฉดสี

ถ้าเลือกสีแล้ววาดผิดสี ให้เพิ่มค่า

---

## 38. `Row delay`

พักหลังวาดครบหนึ่งแถว

ช่วยลดอาการเกมรับคลิกไม่ทัน

---

## 39. `Stroke step delay`

ดีเลย์ระหว่างจุดในจังหวะวาดต่อเนื่อง

ใช้กับงานที่วาดแบบลากหรือคลิกถี่

---

## 40. `After stroke delay`

พักหลังจบ Stroke หนึ่งชุด

---

## 41. `Stroke neighbors (rapid clicks)`

ให้โปรแกรมคลิกจุดใกล้เคียงแบบเร็วขึ้น

เหมาะกับ:

- พื้นที่สีเดียวต่อกัน
- งานที่ต้องการลดเวลา

ไม่เหมาะกับ:

- เครื่องหน่วง
- เกมรับคลิกไม่ทัน

---

## 42. `Verify (repaint misses)`

ให้โปรแกรมตรวจว่ามีจุดไหนวาดพลาด แล้วกลับไปซ่อม

เหมาะกับ:

- งานละเอียด
- เครื่องหรือเกมมีโอกาสคลิกไม่ติด

ข้อเสีย:

- ใช้เวลามากขึ้น

---

## 43. `Verify while painting (lag)`

ตรวจระหว่างวาด

ใช้เมื่อ:

- เกมหน่วงมาก
- วาดยาวแล้วกลัวพลาดเยอะ

---

## 44. `Auto-recover verify loops (skip stuck verify)`

ถ้า Verify วนซ้ำเพราะติดจุดเดิม โปรแกรมจะพยายามข้ามเพื่อไม่ให้ค้าง

ควรเปิดไว้ถ้าใช้ Verify

---

## 45. `Show in-game status overlay`

แสดงสถานะบนหน้าจอระหว่างทำงาน

ใช้ดูว่าโปรแกรมกำลังทำอะไร เช่น กำลังวาด กำลังเลือกสี หรือหยุด

---

# Paint

## 46. ช่อง `Method`

เลือกวิธีวาด

| Method | เหมาะกับ |
|---|---|
| `Paint by Row` | วาดเรียงแถว เหมาะกับภาพทั่วไป |
| `Paint by Color` | วาดทีละสี เหมาะกับภาพที่มีสีซ้ำเยอะ |

ตัวอย่าง:

- รูปภาพจริง ใช้ `Paint by Row`
- โลโก้หรือลายผ้า ใช้ `Paint by Color`

---

## 47. `Bucket-fill most-used color first`

เติมสีที่ใช้เยอะที่สุดก่อนด้วยถังสี

เหมาะกับ:

- พื้นหลังสีเดียว
- พื้นที่ใหญ่ ๆ

ตัวอย่าง:

ถ้ารูปมีพื้นหลังฟ้า 70% โปรแกรมจะเติมฟ้าก่อน แล้วค่อยวาดรายละเอียด

---

## 48. Threshold ของ Bucket-fill most-used color

กำหนดว่าพื้นที่สีต้องเยอะแค่ไหนถึงควรใช้ถังสี

ถ้าตั้งต่ำเกิน:

- โปรแกรมอาจใช้ถังสีกับพื้นที่ไม่ใหญ่จริง

ถ้าตั้งสูงเกิน:

- โปรแกรมอาจไม่ใช้ถังสีเลย

---

## 49. `Bucket-fill large regions (outline first)`

เติมพื้นที่สีใหญ่โดยวาดขอบก่อน

เหมาะกับ:

- พื้นที่สีเดียวที่มีขอบชัด
- ลดจำนวนคลิก

---

## 50. ปุ่ม `Paint now`

เริ่มวาดจริงในเกม

ก่อนกดต้องเช็ก:

1. เปิดหน้าเกมถูกหน้าแล้ว
2. เลือก Canvas area แล้ว
3. ตั้งปุ่มเครื่องมือแล้ว
4. ตั้งสีแล้ว
5. หน้าต่างเกมอยู่ตำแหน่งเดิม

---

## 51. ปุ่ม `Resume`

วาดต่อจากจุดที่หยุดไว้

ใช้เมื่อ:

- หยุดกลางคัน
- โปรแกรม Pause
- ต้องการทำต่อโดยไม่เริ่มใหม่

---

## 52. ปุ่ม `Erase canvas`

ลบภาพบน Canvas ในเกม

ก่อนใช้ควรตั้ง:

- `Set eraser tool button`
- `Set eraser thickness + button`
- Canvas area ให้ถูกต้อง

ข้อควรระวัง:

- ปุ่มนี้ควบคุมเมาส์ลบจริงในเกม
- ถ้าพื้นที่ Canvas ผิด อาจลบนอกตำแหน่ง

---

## 53. ปุ่ม `Stop`

หยุดการทำงานของโปรแกรม

ควรใช้ทันทีถ้า:

- เมาส์เริ่มคลิกผิดที่
- เกมเปลี่ยนหน้า
- มีหน้าต่างอื่นเด้งมาบัง

---

# โปรแกรมที่ 2: Clothing Template Manager

## 54. หน้าที่ของ Template Manager

โปรแกรมนี้ใช้เตรียมรูปให้พอดีกับเสื้อผ้าในเกม ก่อนนำไปวาดด้วยโปรแกรมหลัก

เหมาะกับ:

- Dress
- T-Shirt
- แยก Import รูปแต่ละส่วน
- ตัดภาพตาม Mask
- Extract Mask จาก Screenshot เกม
- ขยับรูปก่อน Export
- Export รูปแยก Part

หน้าตาใหม่จัดเป็น 4 ขั้นตอนหลัก:

1. `Step 1 - Choose preset` เลือกประเภทก่อน เช่น Dress หรือ T-Shirt
2. `Step 2 - Pick part and image` เลือก Part และ Import รูป
3. `Step 3 - Fit and position` จัดขนาดและตำแหน่งรูป
4. `Step 4 - Export` บันทึกไฟล์สำหรับนำไปวาดในโปรแกรมหลัก

ส่วน `Optional - Mask, skeleton and calibration` เป็นขั้นสูง ใช้เฉพาะตอนต้องแก้ทรงชุดหรือ Mapping ไม่ตรง

---

## 55. วิธีเปิด Template Manager

เปิดด้วยไฟล์:

```powershell
python dress_template_manager.py
```

---

## 56. ปุ่ม `Import image for this part...`

Import รูปสำหรับ Part ที่เลือกอยู่เท่านั้น

ตัวอย่าง:

ถ้าเลือก `Dress > Front Part` แล้ว Import รูปกระต่าย รูปนี้จะใช้กับ Front Part เท่านั้น

เหมาะกับ:

- แต่ละ Part ใช้รูปคนละรูป
- ด้านหน้าและด้านหลังชุดใช้ลายต่างกัน

---

## 57. ปุ่ม `Use this image for all parts in preset`

ใช้รูปปัจจุบันกับทุก Part ใน Preset เดียวกัน

ตัวอย่าง:

ถ้า Preset เป็น `Dress` แล้วกดปุ่มนี้ รูปเดียวกันจะถูกใช้กับ:

- Front Part
- Back Part
- Innerwear

เหมาะกับ:

- อยากใช้รูปเดียวครอบคลุมทั้งชุด
- ทำลายผ้าซ้ำ ๆ

---

## 58. ปุ่ม `Clear current part image`

ลบรูปของ Part ที่เลือกอยู่

ใช้เมื่อ:

- Import รูปผิด
- อยากให้ Part นั้นว่าง

---

## 59. แถบ `Choose clothing preset`

เลือก Preset เสื้อผ้าก่อนเริ่มทำงาน โปรแกรมจะโหลดเฉพาะ Part และค่าของ Preset นั้นเท่านั้น

| Preset | มี Part อะไร |
|---|---|
| `Dress` | Front Part, Back Part, Innerwear |
| `T-Shirt` | Front, Back, Left Sleeve, Right Sleeve |

ตัวอย่าง:

- ถ้าเลือก `T-Shirt` โปรแกรมจะแสดงเฉพาะ Front, Back, Left Sleeve, Right Sleeve
- ถ้าเลือก `Dress` โปรแกรมจะแสดงเฉพาะ Front Part, Back Part, Innerwear
- รูปของ Dress จะไม่ถูกดึงไปใช้กับ T-Shirt อัตโนมัติ

นี่คือจุดเริ่มต้นของโปรแกรมใหม่ ควรเลือก Preset ให้ถูกก่อน Import รูป

---

## 60. ช่อง `Part`

เลือกส่วนที่จะจัดภาพ

สำคัญมาก:

Preset และ Part ใน Template Manager ต้องตรงกับ Preset และ Part ที่จะวาดในเกม และตรงกับ Part ในโปรแกรมหลัก

ตัวอย่าง:

ถ้าจะเตรียมด้านหลัง Dress ให้เลือก:

- Preset: `Dress`
- Part: `Back Part`

---

## 61. ช่อง `Fit`

กำหนดวิธีใส่รูปลงใน Template

| ค่า | ความหมาย |
|---|---|
| `Cover / Fill part` | ขยายให้เต็ม Part อาจตัดบางส่วน |
| `Contain full image` | เห็นรูปครบ อาจมีพื้นที่ว่าง |
| `Stretch exact` | ยืดให้พอดีเป๊ะ แต่สัดส่วนอาจเพี้ยน |

ตัวอย่าง:

- รูปหน้าสัตว์ ใช้ `Cover / Fill part`
- รูปตัวละครเต็มตัว ใช้ `Contain full image`
- ลาย Pattern ใช้ `Stretch exact` หรือ `Cover / Fill part`

---

## 62. `Auto-crop empty border`

ตัดขอบว่างของรูปก่อนใส่ Template

ควรเปิดเมื่อรูปมีขอบว่างเยอะ

ควรปิดเมื่อรูปถูกจัดตำแหน่งไว้แล้ว

---

## 63. `Clip to mask / transparent shape`

ตัดรูปให้เหลือเฉพาะทรงของเสื้อผ้า

ควรเปิดเมื่อ:

- ต้องการให้พื้นที่นอกชุดโปร่งใส
- ต้องการเห็นว่าเส้นขอบชุดจริงอยู่ตรงไหน

ถ้าปิด:

- รูปจะเป็นสี่เหลี่ยมเต็มไฟล์

---

## 64. ปุ่ม `Import mask for current part...`

นำ Mask ที่เตรียมไว้เข้ามาใช้กับ Part ปัจจุบัน

ใช้เมื่อ:

- มีไฟล์ Mask ที่ตรงกับเกมแล้ว
- ต้องการใช้ Mask เฉพาะของตัวเอง

---

## 65. ปุ่ม `Extract mask from game screenshot...`

ดึง Mask จาก Screenshot ของเกม

นี่คือปุ่มสำคัญสำหรับแก้ปัญหา Mapping ไม่ตรง

วิธีใช้:

1. เข้าเกมหน้า Dress หรือ T-Shirt
2. เลือก Part ที่ต้องการ เช่น `Front Part`
3. ให้เห็นพื้นที่สีส้มและเส้นขาวของเกมชัดเจน
4. ถ่าย Screenshot โดยไม่วาดเส้นสีแดงทับ
5. กลับมา Template Manager
6. เลือก Preset และ Part ให้ตรง
7. กด `Extract mask from game screenshot...`
8. เลือกไฟล์ Screenshot
9. ดู Preview ว่าเส้น Mask ตรงกับทรงเกมหรือไม่

ข้อควรระวัง:

- Screenshot ต้องไม่มีรอยวงแดงหรือเส้นเขียนทับ
- ต้องเห็นขอบขาวของเกมครบ
- ถ้าเลือก Part ผิด Mask จะผิด
- ถ้า Screenshot ถูก Crop แปลก ๆ อาจ Extract พลาด

---

## 66. ปุ่ม `Clear current part mask`

ลบ Mask ของ Part ปัจจุบัน

ใช้เมื่อ:

- Extract Mask ผิด
- ต้องการกลับไปใช้ Mask เริ่มต้น

---

# Mask Calibration

## 67. `Mask Scale X`

ขยายหรือย่อ Mask แนวนอน

ใช้เมื่อ:

- ไหล่ซ้ายขวาไม่ตรง
- ขอบชุดกว้างหรือแคบกว่าเกม

ตัวอย่าง:

ถ้าขอบ Mask แคบกว่าเกม ให้เพิ่ม `Mask Scale X`

---

## 68. `Mask Scale Y`

ขยายหรือย่อ Mask แนวตั้ง

ใช้เมื่อ:

- ช่วงคอหรือชายชุดสูงต่ำไม่ตรง
- Mask สั้นหรือยาวกว่าเกม

---

## 69. `Mask Offset X`

ขยับ Mask ซ้ายหรือขวา

| อาการ | วิธีแก้ |
|---|---|
| Mask อยู่ซ้ายเกิน | เพิ่ม Offset X |
| Mask อยู่ขวาเกิน | ลด Offset X |

---

## 70. `Mask Offset Y`

ขยับ Mask ขึ้นหรือลง

| อาการ | วิธีแก้ |
|---|---|
| Mask สูงเกิน | เพิ่ม Offset Y |
| Mask ต่ำเกิน | ลด Offset Y |

---

## 71. ปุ่ม `Reset mask calibration`

รีเซ็ตค่า Scale และ Offset ของ Mask

ใช้เมื่อ:

- ปรับผิดทิศ
- ต้องการเริ่ม Calibrate ใหม่

---

# Flexible Skeleton Mapping

## 72. `Flexible skeleton mapping (fit rows into mask)`

ใช้ให้โปรแกรมยืด/บีบรูปตามโครง Mask ทีละแถว แทนการเอารูปสี่เหลี่ยมไปตัดด้วย Mask เฉย ๆ

เหมาะกับ:

- Dress ที่ไหล่หรือสายเดี่ยวไม่ตรง
- รูปที่ต้องการให้ “กินพื้นที่ในทรงชุด” มากขึ้น
- เคสที่ภาพตรงกลางถูกแล้ว แต่ส่วนขอบชุดดูไม่เข้ารูป

ต้องมี Mask ก่อนจึงจะเห็นผล:

- เปิด `Clip to mask / transparent shape`
- หรือ Import/Extract Mask จากเกม

ถ้าไม่เปิด Mask ตัวเลือกนี้จะไม่มีผล

---

## 73. `Skeleton Strength`

กำหนดแรงในการยืดรูปเข้าโครง Mask

| ค่า | ผลลัพธ์ |
|---|---|
| `0%` | ไม่ยืดตามโครง เหมือนเดิม |
| `50%` | ยืดครึ่งหนึ่ง ภาพเพี้ยนน้อยกว่า |
| `100%` | ยืดเข้าโครงเต็มที่ เหมาะกับแก้ทรงชุด |

คำแนะนำ:

- เริ่มที่ `70-100%` สำหรับ Dress
- ถ้าหน้าหรือรายละเอียดบิดเบี้ยวเกินไป ลดลงมาที่ `40-60%`

---

## 74. `Skeleton Edge Padding`

ขยับขอบอ้างอิงของ Skeleton เข้าหรือออก

| ค่า | ผลลัพธ์ |
|---|---|
| ค่าบวก | บีบรูปเข้าด้านใน เหลือขอบปลอดภัยมากขึ้น |
| ค่า 0 | ใช้ขอบ Mask จริง |
| ค่าลบ | ขยายการยืดออกนอกขอบมากขึ้น แต่ Mask ยังตัดภาพนอกทรงอยู่ |

ตัวอย่าง:

ถ้าลายกินถึงขอบไหล่มากเกินไป ให้เพิ่ม Padding เช่น `2-5 px`

---

## 75. ปุ่ม `Reset skeleton mapping`

ปิดและรีเซ็ตค่า Skeleton Mapping ของ Part ปัจจุบัน

ใช้เมื่อ:

- ภาพบิดเกินไป
- ต้องการกลับไปใช้การวางรูปแบบเดิม

---

# Image Transform ใน Template Manager

## 76. `Background: #FFFFFF`

กำหนดสีพื้นหลังที่ใช้แทนพื้นที่โปร่งใส

ถ้า Export แบบมี Transparency และนำไปวาดในเกม โปรแกรมหลักจะใช้ข้อมูลภาพตามที่เตรียมไว้

---

## 77. `Image Scale`

ขยายหรือย่อรูปภายใน Template

ต่างจาก `Mask Scale`:

- `Image Scale` ขยับหรือขยายรูปของเรา
- `Mask Scale` ขยับหรือขยายทรงเสื้อผ้า

ตัวอย่าง:

ถ้าหน้ากระต่ายเล็กเกินในชุด ให้เพิ่ม `Image Scale`

---

## 78. `Image Offset X`

ขยับรูปซ้ายหรือขวาใน Template

ตัวอย่าง:

ถ้าตากระต่ายอยู่ซ้ายเกิน ให้เพิ่ม Offset X เพื่อขยับรูปไปขวา

---

## 79. `Image Offset Y`

ขยับรูปขึ้นหรือลงใน Template

ตัวอย่าง:

ถ้าหน้าสัตว์ต่ำเกินไป ให้ลด Offset Y เพื่อขยับขึ้น

---

## 80. ปุ่ม `Reset current part`

รีเซ็ตค่ารูปของ Part ปัจจุบัน

ใช้เมื่อ:

- Scale/Offset ของรูปเพี้ยน
- ต้องการเริ่มจัดใหม่

---

## 81. ปุ่ม `Copy to all parts in this preset`

คัดลอกค่ารูปและการตั้งค่าจาก Part ปัจจุบัน ไปยังทุก Part ใน Preset เดียวกัน

ตัวอย่าง:

ถ้าอยู่ที่ `Dress > Front Part` แล้วกดปุ่มนี้ ค่าอาจถูกคัดลอกไปยัง:

- Back Part
- Innerwear

เหมาะกับ:

- ทำลายซ้ำทั้งชุด
- เริ่มจากค่ากลางเดียวกันก่อน แล้วค่อยปรับแต่ละ Part

---

## 82. ปุ่ม `Copy to every preset`

คัดลอกค่าไปทุก Preset เสื้อผ้า

ปุ่มนี้ถูกซ่อนจากหน้าหลักใน UI ใหม่ เพราะแนวทางใหม่คือเลือก Preset ก่อน แล้วทำงานเฉพาะ Preset นั้นเพื่อลดการปนกันระหว่าง T-Shirt กับ Dress

---

# Export ใน Template Manager

## 79. ปุ่ม `Choose output folder...`

เลือกโฟลเดอร์ที่จะบันทึกไฟล์ Export

แนะนำ:

ใช้โฟลเดอร์ที่หาเจอง่าย เช่น `Downloads`

---

## 80. ปุ่ม `Export current part`

Export เฉพาะ Part ที่เลือกอยู่

ตัวอย่าง:

ถ้าเลือก `Dress > Front Part` จะได้ไฟล์สำหรับ Front Part เท่านั้น

---

## 81. ปุ่ม `Export current preset`

Export ทุก Part ของ Preset ปัจจุบัน

ตัวอย่าง:

ถ้า Preset เป็น `Dress` จะ Export:

- Front Part
- Back Part
- Innerwear

---

## 82. ปุ่ม `Export all presets`

Export ทุก Preset ที่โปรแกรมรองรับ

ปุ่มนี้ถูกซ่อนจากหน้าหลักใน UI ใหม่ เพื่อบังคับให้ Export ตาม Preset ที่เลือกอยู่เป็นหลัก ใช้แนวคิดนี้แทน:

- เลือก Preset ที่ต้องการ
- ตรวจ Part ให้ครบ
- กด `Export current preset`

---

## 83. ปุ่ม `Open output folder`

เปิดโฟลเดอร์ที่ Export ไว้

ใช้หลัง Export เพื่อดูไฟล์ผลลัพธ์ทันที

---

# Preview ใน Template Manager

## 84. พื้นที่ Preview ใหญ่

ใช้ดูผลลัพธ์ของ Part ปัจจุบัน

ควรตรวจ:

- รูปอยู่ในตำแหน่งที่ต้องการไหม
- ขอบ Mask ตัดถูกไหม
- ส่วนสำคัญ เช่น หน้า ตา ตัวหนังสือ ไม่โดนตัดไหม

---

## 85. การลากรูปใน Preview

ลากเมาส์บน Preview เพื่อขยับรูป

เหมาะกับ:

- จัดหน้าให้อยู่กลางชุด
- ขยับลายให้ตรงกับตำแหน่งไหล่หรือหน้าอก

---

## 86. Mouse wheel ใน Preview

หมุนล้อเมาส์เพื่อย่อหรือขยายรูป

ตัวอย่าง:

- หมุนขึ้นเพื่อขยาย
- หมุนลงเพื่อย่อ

---

## 87. Thumbnail ด้านล่าง

ใช้ดูภาพรวมของแต่ละ Part

ถ้าทำ Dress ควรเช็กครบ:

- Front Part
- Back Part
- Innerwear

---

# ตัวอย่างการใช้งานจริง

## 88. ตัวอย่างที่ 1: วาดรูปทั่วไป 1:1

สถานการณ์:

ต้องการวาดรูปแมวลงกรอบ 1:1

ขั้นตอน:

1. เปิดเกมไปหน้า New Drawing
2. เลือกขนาด 1:1 ในเกม
3. เปิด `Heartopia Image Painter`
4. กด `Import image...`
5. เลือกรูปแมว
6. ตั้ง `Canvas preset` เป็น `1:1`
7. เลือก `Precision` ตามที่เลือกในเกม
8. กด `Select canvas area...`
9. ลากครอบพื้นที่วาดในเกม
10. ตั้งค่าปุ่มสีและเครื่องมือ
11. กด `Paint now`

ถ้าภาพไม่ชัด:

- เพิ่ม Precision
- เปิด Dither
- จับ Palette ใหม่ให้ครบสีขึ้น

---

## 89. ตัวอย่างที่ 2: วาด Dress Front Part จากรูปเดียว

สถานการณ์:

ต้องการเอารูปกระต่ายไปวางบน Dress Front Part

ขั้นตอนใน Template Manager:

1. เปิด `Clothing Template Manager`
2. เลือก Preset `Dress`
3. เลือก `Part: Front Part`
4. กด `Import image for selected part...`
5. เลือกรูปกระต่าย
6. ใช้ `Image Scale` ขยายให้เต็มชุด
7. ใช้ `Image Offset X/Y` ขยับให้หน้ากระต่ายอยู่กลาง
8. เปิด `Clip to mask / transparent shape`
9. กด `Export current part`

ขั้นตอนในโปรแกรมหลัก:

1. เปิด `Heartopia Image Painter`
2. Import ไฟล์ที่ Export มา
3. เลือก `Canvas preset: Dress`
4. เลือก `Part: Front Part`
5. ในเกมเลือก Dress และ Front Part ให้ตรง
6. กด `Select canvas area...`
7. ลากพื้นที่วาดในเกม
8. กด `Paint now`

---

## 90. ตัวอย่างที่ 3: ทำ Dress ครบ 3 ส่วน

สถานการณ์:

ต้องการทำ Dress มีลายต่อเนื่องกัน

ขั้นตอน:

1. เปิด Template Manager
2. เลือก `Dress`
3. เตรียม `Front Part`
4. เตรียม `Back Part`
5. เตรียม `Innerwear`
6. กด `Export current preset`
7. เปิดโปรแกรมหลัก
8. วาดในเกมทีละ Part

ลำดับที่แนะนำ:

1. Front Part
2. Back Part
3. Innerwear

เหตุผล:

Front Part เห็นผลชัดที่สุด ถ้า Mapping ยังไม่ตรง จะรู้เร็ว

---

## 91. ตัวอย่างที่ 4: Extract Mask จาก Screenshot เกม

สถานการณ์:

ขอบไหล่ Dress ไม่ตรงกับ Template เดิม

ขั้นตอน:

1. เข้าเกมหน้า Dress Front Part
2. ให้พื้นที่วาดสีส้มและเส้นขาวแสดงครบ
3. ถ่าย Screenshot แบบไม่ขีดเขียนทับ
4. เปิด Template Manager
5. เลือก `Dress > Front Part`
6. กด `Extract mask from game screenshot...`
7. เลือก Screenshot
8. ดู Preview ว่า Mask ตรงขึ้นหรือไม่
9. ถ้ายังเหลื่อม ให้ใช้ `Mask Scale X/Y` และ `Mask Offset X/Y`
10. Export ใหม่

---

## 92. ตัวอย่างที่ 5: รูปตรงกลางแล้ว แต่ไหล่ไม่ตรง

อาการ:

- ลำตัวตรง
- คอตรง
- ชายชุดตรง
- แต่ไหล่ซ้ายขวาเหลื่อม

วิธีแก้ที่ถูกต้อง:

1. อย่าขยับ Image Offset ก่อน
2. ตรวจว่า Mask มาจาก Screenshot จริงของ Part นั้นหรือยัง
3. ถ้ายังใช้ Mask เริ่มต้น ให้ Extract Mask จาก Screenshot ก่อน
4. ถ้า Extract แล้วแต่ยังเหลื่อมเล็กน้อย ให้ปรับ `Mask Scale X`
5. ถ้าเหลื่อมไปทั้งก้อน ให้ปรับ `Mask Offset X`
6. Export แล้วทดสอบใหม่

หลักการ:

ถ้ารูปอยู่ถูก แต่ขอบชุดผิด ปัญหาอยู่ที่ Mask ไม่ใช่รูป

---

## 93. ตัวอย่างที่ 6: รูปไม่เต็มชุด

อาการ:

รูปอยู่กลางชุด แต่เหลือขอบว่างเยอะ

วิธีแก้:

- เพิ่ม `Image Scale`
- หรือเปลี่ยน `Fit` เป็น `Cover / Fill part`

ถ้าขยายแล้วหัวโดนตัด:

- ลด `Image Scale`
- ใช้ `Image Offset Y` ขยับลง

---

## 94. ตัวอย่างที่ 7: วาดแล้วโปรแกรมคลิกผิดที่

เช็กตามนี้:

1. เกมอยู่ตำแหน่งเดิมตอนที่ Select canvas area หรือไม่
2. หน้าต่างเกมถูกย่อหรือขยับไหม
3. Zoom ในเกมเปลี่ยนไหม
4. Preset และ Part ตรงกับเกมไหม
5. โปรแกรมรันด้วยสิทธิ์พอหรือไม่
6. มีหน้าต่างโปรแกรมบังพื้นที่เกมหรือไม่

ถ้าไม่แน่ใจ:

1. กด Stop
2. เลือก Canvas area ใหม่
3. ทดสอบด้วยรูปเล็กก่อน

---

# Cheat Sheet แก้ปัญหาเร็ว

## 95. ภาพเคลื่อนทั้งภาพ

| อาการ | ปรับอะไร |
|---|---|
| ภาพไปซ้าย | เพิ่ม `Image Offset X` |
| ภาพไปขวา | ลด `Image Offset X` |
| ภาพสูงไป | เพิ่ม `Image Offset Y` |
| ภาพต่ำไป | ลด `Image Offset Y` |

---

## 96. ขอบชุดไม่ตรง แต่รูปอยู่ถูก

| อาการ | ปรับอะไร |
|---|---|
| ขอบแคบหรือกว้างผิด | `Mask Scale X` |
| ขอบสูงหรือต่ำผิด | `Mask Scale Y` |
| Mask เลื่อนไปซ้ายขวา | `Mask Offset X` |
| Mask เลื่อนขึ้นลง | `Mask Offset Y` |

---

## 97. สีเพี้ยน

แก้ตามลำดับ:

1. เปิด `Map preview to captured palette`
2. ดูว่า Preview เพี้ยนตั้งแต่ก่อนวาดหรือไม่
3. ถ้าแดงน้ำเงินสลับ ให้ใช้ `Fix colors: swap R/B`
4. ถ้าสีไม่พอ ให้ Setup สีเพิ่ม
5. ถ้าเป็นรูปภาพจริง ให้ลองเปิด Dither

---

## 98. โปรแกรมวาดช้าเกิน

ลองปรับ:

- ใช้ `Paint by Color`
- เปิด Bucket-fill
- ลด Verify
- ลด Precision
- ลด Delay ถ้าเครื่องรับไหว

---

## 99. โปรแกรมวาดพลาด

ลองปรับ:

- เพิ่ม `After each click delay`
- เพิ่ม `Mouse down hold`
- เปิด `Verify`
- เปิด `Auto-recover verify loops`
- อย่าให้หน้าต่างอื่นบังเกม

---

# คำแนะนำสำหรับการทดสอบ

## 100. ก่อนวาดงานใหญ่

ให้ทำ Test เล็กก่อนเสมอ:

1. ใช้รูปง่าย ๆ
2. วาดแค่ Part เดียว
3. ใช้ Precision ต่ำก่อน
4. ตรวจตำแหน่ง
5. ถ้าตรงแล้วค่อยวาดจริง

---

## 101. ถ้าทดสอบ Dress

แนะนำทดสอบตามลำดับ:

1. Front Part เพราะเห็นปัญหาง่ายสุด
2. Back Part
3. Innerwear

ในการทดสอบแต่ละครั้งให้สังเกต:

- คอตรงไหม
- ไหล่ตรงไหม
- ชายชุดตรงไหม
- ภาพถูกตัดตามขอบไหม
- พื้นที่นอกชุดมีสีหลุดไหม

---

## 102. การหยุดฉุกเฉิน

ถ้าเมาส์เริ่มคลิกผิด:

1. กด `Stop`
2. หรือเลื่อนเมาส์ไปมุมจอถ้า PyAutoGUI fail-safe ทำงาน
3. หรือสลับหน้าต่างแล้วปิดโปรแกรม

---

# สรุปการเลือกเครื่องมือ

## ถ้าจะวาดรูปทั่วไป

ใช้แค่:

- `Heartopia Image Painter`
- `Import image...`
- `Canvas preset`
- `Precision`
- `Select canvas area...`
- `Paint now`

## ถ้าจะวาด Dress หรือ T-Shirt ให้ตรงทรง

ใช้ทั้ง 2 โปรแกรม:

1. `Clothing Template Manager` เพื่อเตรียมรูป
2. `Heartopia Image Painter` เพื่อวาดลงเกม

## ถ้า Mapping ไม่ตรง

ให้แก้ในลำดับนี้:

1. ตรวจว่าเลือก Preset และ Part ตรงเกม
2. ใช้ `Extract mask from game screenshot...`
3. ปรับ `Mask Scale / Offset`
4. Export ใหม่
5. Import เข้าโปรแกรมหลักแล้ววาดทดสอบ

---

# ข้อควรจำสำคัญ

- โปรแกรมคลิกเมาส์จริง จึงต้องเลือกพื้นที่ให้ถูก
- ถ้าเกมขยับหน้าต่าง ต้อง Select canvas area ใหม่
- ถ้าเปลี่ยน Zoom ในเกม ต้องตรวจ Canvas ใหม่
- ถ้าวาดเสื้อผ้า อย่าเดา Mask ให้ใช้ Screenshot ของเกมจริง
- ถ้ารูปตรงแต่ขอบไม่ตรง ปัญหามักอยู่ที่ Mask
- ถ้าขอบตรงแต่รูปไม่ตรง ปัญหาอยู่ที่ Image Scale หรือ Image Offset
- ถ้าคลิกในเกมไม่ได้ ให้ลองรันโปรแกรมแบบ Administrator
